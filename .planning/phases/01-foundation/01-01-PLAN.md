---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/taxonomy-criteria.md
autonomous: true

must_haves:
  truths:
    - "四层认知结构的判断标准文档存在且可用"
    - "每个层级（领域/子领域/主题/知识点）有明确的界定规则"
    - "文档包含具体示例和边界情况处理指南"
    - "后续阶段可以依据此文档做出一致的分类决策"
  artifacts:
    - path: ".planning/taxonomy-criteria.md"
      provides: "四层认知结构判断标准文档"
      contains:
        - "领域(Domain)判断标准"
        - "子领域(Subdomain)判断标准"
        - "主题(Topic)判断标准"
        - "知识点(Knowledge Point)判断标准"
        - "决策树和边界案例"
  key_links:
    - from: "ROADMAP.md"
      to: "taxonomy-criteria.md"
      via: "reference"
      pattern: "taxonomy-criteria.md"
---

<objective>
Create the four-layer cognitive hierarchy taxonomy criteria document that defines clear decision rules for classifying content into Domain, Subdomain, Topic, and Knowledge Point levels.

Purpose: Establish consistent classification standards that all subsequent migration phases will follow, ensuring uniform structure across all 46 domains.
Output: `.planning/taxonomy-criteria.md` with decision trees, concrete examples, and boundary case handling.
</objective>

<execution_context>
@/Users/lionad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lionad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

# Key decisions from CONTEXT.md (LOCKED)
- 领域 (Domain): 按照 0.index.md 中的 20+ 分类，每个领域对应一个以 `_` 为前缀的目录
- 子领域 (Subdomain): 统一使用目录形式，不按内容量区分文件或目录
- 主题 (Topic): 界定标准是"优先按照讨论范围"。有扩展潜力或内容超过 150+ 行时拆分
- 知识点 (Knowledge Point): 观点、案例、洞见等思考性内容，保持为四级标题
- 现有文档的三级标题处理: 重新审视每个三级标题，决定其所属层级

# Research findings from RESEARCH.md
- Decision tree documentation pattern recommended
- Include concrete examples for each level
- Document boundary case handling
- 325 files distributed as: 171 simple (<50 lines), 96 medium (50-150 lines), 57 complex (150+ lines)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create taxonomy criteria document structure</name>
  <files>.planning/taxonomy-criteria.md</files>
  <action>
Create `.planning/taxonomy-criteria.md` with the following structure:

1. **Header section**: Title, version, last updated date

2. **Overview**: Brief explanation of the 4-layer hierarchy and its purpose

3. **Domain (领域) section**:
   - Definition and scope
   - Decision criteria (from locked decisions)
   - Physical representation (directory with `_` prefix)
   - Examples from existing codebase (list 3-5 actual domain directories)
   - Verification method

4. **Subdomain (子领域) section**:
   - Definition and scope
   - Decision criteria (unified directory form)
   - Physical representation (subdirectory with entry file)
   - Required files (`0.index.md` or `{subdomain}.md`)
   - Examples showing directory structure

5. **Topic (主题) section**:
   - Definition and scope
   - Decision criteria (discussion scope priority, 150+ line threshold, extension potential)
   - Decision tree diagram (text-based)
   - Physical representation (Markdown file)
   - Examples showing when to split vs. keep together

6. **Knowledge Point (知识点) section**:
   - Definition and scope
   - Decision criteria (opinions, cases, insights - not factual descriptions)
   - Physical representation (H4 heading `####`)
   - Examples of correct H4 usage
   - Anti-patterns (what NOT to make a knowledge point)

7. **Heading Level Guidelines**:
   - H1 (`#`): File title
   - H2 (`##`): Topic subcategories or chapters
   - H3 (`###`): Potential subtopics (evaluate for promotion)
   - H4 (`####`): Knowledge points

8. **Decision Trees**:
   - Visual/text flowchart for "Is this a Topic or Knowledge Point?"
   - Visual/text flowchart for "Should this H3 be promoted to a file?"

9. **Boundary Cases**:
   - Content exactly at 150 lines
   - Content that could be either subdomain or topic
   - Edge cases for knowledge point classification
   - Claude's discretion guidelines

10. **Examples Gallery**:
    - 2-3 concrete examples from actual codebase showing before/after
    - One simple domain example
    - One medium domain example

Reference actual content from `content/6.maps/` to provide realistic examples.
  </action>
  <verify>
    - File exists at `.planning/taxonomy-criteria.md`
    - File contains all 10 required sections
    - File references actual domain names from codebase
    - Decision trees are clear and actionable
  </verify>
  <done>
    Taxonomy criteria document created with complete structure, decision trees, and concrete examples from the codebase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add concrete examples from actual content</name>
  <files>.planning/taxonomy-criteria.md</files>
  <action>
Read actual content files from `content/6.maps/` to extract concrete examples:

1. Read `content/6.maps/0.index.md` to understand domain classification

2. Read 2-3 simple domain files (e.g., `_go/go.md`, `_php/php.md`) to show simple structure

3. Read 1-2 medium domain entries (e.g., `_business/business.md`) to show medium complexity

4. Read 1 complex domain structure (e.g., `_frontend/css/layers.md`) to show nested structure

5. Extract specific examples of:
   - Good H4 knowledge points
   - H3 headings that should be topics
   - H3 headings that should stay as headings
   - Proper subdomain organization

6. Update the taxonomy-criteria.md with these concrete examples, including:
   - File paths
   - Line counts
   - Heading structures
   - Classification rationale

Ensure examples are real and verifiable from the codebase.
  </action>
  <verify>
    - Examples reference actual file paths from content/6.maps/
    - Examples include specific line counts and heading structures
    - At least one example from each complexity level (simple, medium, complex)
  </verify>
  <done>
    Document contains concrete, verifiable examples from actual codebase content.
  </done>
</task>

</tasks>

<verification>
1. Document exists at `.planning/taxonomy-criteria.md`
2. Document contains all required sections per structure
3. Decision trees are actionable without interpretation
4. Examples are from actual codebase (not hypothetical)
5. Boundary cases provide clear guidance
6. Document can be used by subsequent phases to make consistent decisions
</verification>

<success_criteria>
- Taxonomy criteria document exists and is complete
- All four hierarchy levels have clear decision criteria
- Decision trees provide unambiguous guidance
- Concrete examples from codebase demonstrate each level
- Boundary cases are documented with handling guidance
- Document is ready for reference in Phase 2-18
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- What was created (taxonomy-criteria.md location and purpose)
- Key decision criteria documented
- Examples included
- How to use the document in subsequent phases
</output>

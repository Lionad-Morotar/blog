---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/link-audit.md
  - .planning/link-update-strategy.md
autonomous: true

must_haves:
  truths:
    - "所有内部链接已审计并记录（298+ 处）"
    - "链接按类型分类（内部/跨目录/导航）"
    - "链接更新策略文档化（直接更新方案）"
    - "每个链接都有对应的迁移行动计划"
  artifacts:
    - path: ".planning/link-audit.md"
      provides: "完整链接审计清单"
      contains:
        - "所有 /maps/ 链接清单"
        - "源文件位置"
        - "目标路径"
        - "链接类型分类"
        - "迁移行动"
    - path: ".planning/link-update-strategy.md"
      provides: "链接更新策略文档"
      contains:
        - "直接更新链接方案"
        - "grep 查找模式"
        - "更新优先级"
        - "验证方法"
  key_links:
    - from: "link-audit.md"
      to: "link-update-strategy.md"
      via: "reference"
      pattern: "link-update-strategy.md"
---

<objective>
Complete link audit of all internal references to 6.maps/ content and document the direct link update strategy (no redirects).

Purpose: Create a comprehensive inventory of all 298+ internal links that will need updating during migration, and establish the systematic approach for updating them.
Output:
- `.planning/link-audit.md` - Complete inventory of all internal links
- `.planning/link-update-strategy.md` - Strategy document for direct link updates
</objective>

<execution_context>
@/Users/lionad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lionad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

# Key decisions from CONTEXT.md (LOCKED)
- 不保留旧 URL，不采用重定向方案
- 移动文件时，同步更新所有引用该文件的链接
- 采用直接修改链接地址的方式
- 不使用 Nuxt 路由规则、中间件重定向、服务器路由重定向

# Research findings from RESEARCH.md
- 298 internal /maps/ links found across 71 files
- Link patterns: Absolute /maps/ links, relative .md links, cross-directory links
- Grep commands for link discovery documented
- Nuxt Content v2 does NOT automatically rewrite .md links
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate complete link inventory</name>
  <files>.planning/link-audit.md</files>
  <action>
Execute grep commands to find all internal links and create comprehensive inventory:

1. Find all absolute /maps/ links:
   ```bash
   grep -rnE '\]\(/maps/[^)]+\)' /Users/lionad/Github/Lionad-Morotar/blog/content/ --include="*.md"
   ```

2. Find all relative .md links:
   ```bash
   grep -rnE '\]\([^)]+\.md[^)]*\)' /Users/lionad/Github/Lionad-Morotar/blog/content/ --include="*.md"
   ```

3. Create `.planning/link-audit.md` with:
   - Header: Title, generation date, total counts

   - Section 1: Absolute /maps/ Links
     * Table format: | Source File | Line | Link Text | Current Target | Link Type | Migration Action |
     * Link types: internal (within 6.maps), cross-directory (from outside 6.maps), navigation (in 0.index.md)
     * Include all 298+ links found

   - Section 2: Relative .md Links
     * Table format: | Source File | Line | Link Text | Current Target | Context |
     * Focus on links within 6.maps/ directory

   - Section 3: Cross-Directory Links Summary
     * Links from 2.articles/, 7.tools/, 8.source-code/, _achieved/
     * These need special attention during migration

   - Section 4: Navigation Links (0.index.md)
     * All links in the main domain navigation
     * These will need updating as domain structures change

   - Section 5: Statistics
     * Total links by type
     * Links by source directory
     * Links by target domain

4. Categorize each link with migration action:
   - `update-path`: Link path needs updating due to restructuring
   - `verify`: Link should be verified after migration
   - `manual-review`: Link needs human decision

Ensure the inventory is complete and accurate.
  </action>
  <verify>
    - File exists at `.planning/link-audit.md`
    - Total link count matches or exceeds 298
    - All links have source file, line number, and target path
    - Links are categorized by type
    - Cross-directory links are flagged
  </verify>
  <done>
    Complete link inventory created with all 298+ internal links documented and categorized.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document link update strategy</name>
  <files>.planning/link-update-strategy.md</files>
  <action>
Create `.planning/link-update-strategy.md` documenting the direct link update approach:

1. **Header**: Title, version, date, reference to link-audit.md

2. **Strategy Overview**:
   - Explain the "no redirects, direct updates" decision
   - Why this approach was chosen (per CONTEXT.md decisions)
   - Benefits and risks

3. **Pre-Migration Link Mapping**:
   - How to use link-audit.md during migration
   - Mapping old paths to new paths for each domain
   - Template for path mapping

4. **Update During Migration**:
   - Step-by-step process for updating links when moving files
   - Grep commands to find all references to a file before moving:
     ```bash
     grep -r "old/path" content/ --include="*.md"
     ```
   - Priority order for updates:
     1. 0.index.md navigation links (highest visibility)
     2. Cross-directory links (from outside 6.maps/)
     3. Internal 6.maps/ links

5. **Link Update Patterns**:
   - Absolute /maps/ links: `/maps/_domain/file` → `/maps/_domain/subdomain/file`
   - Relative .md links: `./file.md` → `./subdomain/file.md`
   - Cross-directory links: Update to new structure
   - Navigation links: Update domain entries

6. **Verification Methods**:
   - Post-migration grep to confirm no old paths remain
   - Build verification: `nuxt build` or `nuxt generate`
   - Link checker approach
   - Manual spot-checking

7. **Per-Phase Link Update Checklist**:
   - Template checklist for each migration phase
   - Before: Identify all links to affected domains
   - During: Update links as files move
   - After: Verify no broken links

8. **Automation Options**:
   - When to use sed/awk for bulk updates
   - When manual updates are safer
   - Example commands for bulk replacements

9. **Rollback Plan**:
   - How to handle mistakes
   - Git-based recovery approach
   - Emergency reversion process

10. **Appendix: Quick Reference**:
    - Common grep patterns
    - Common sed patterns
    - Verification commands

Reference specific examples from link-audit.md to illustrate patterns.
  </action>
  <verify>
    - File exists at `.planning/link-update-strategy.md`
    - Document explains direct update approach (no redirects)
    - Contains actionable grep/sed commands
    - Includes per-phase checklist template
    - References link-audit.md
  </verify>
  <done>
    Link update strategy documented with clear processes, commands, and verification methods.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create domain path mapping template</name>
  <files>.planning/link-audit.md</files>
  <action>
Add a domain path mapping section to link-audit.md that will be filled in during migration:

1. Create a mapping table for each of the 46 domains:
   ```markdown
   ## Domain Path Mappings

   ### Simple Domains (1 file each)
   | Domain | Old Path | New Path | Status |
   |--------|----------|----------|--------|
   | _go | /maps/_go/go | /maps/_go/go | pending |
   | _markdown | /maps/_markdown/markdown | /maps/_markdown/markdown | pending |
   | ... | ... | ... | ... |

   ### Medium Domains (2-10 files)
   | Domain | Old Path | New Path | Status |
   |--------|----------|----------|--------|
   | _business | /maps/_business/... | /maps/_business/... | pending |
   | ... | ... | ... | ... |

   ### Complex Domains (10+ files)
   | Domain | Old Path | New Path | Status |
   |--------|----------|----------|--------|
   | _ai | /maps/_ai/... | /maps/_ai/... | pending |
   | ... | ... | ... | ... |
   ```

2. For each domain, list the current files (from actual codebase analysis)

3. Leave New Path column empty to be filled during each migration phase

4. Add Status column values: pending, in-progress, complete

5. This mapping will serve as the master reference for all link updates

Use actual file listings from `content/6.maps/` to populate current state.
  </action>
  <verify>
    - Domain path mapping section added to link-audit.md
    - All 46 domains listed
    - Current paths documented from actual codebase
    - New Path columns ready for migration phases
  </verify>
  <done>
    Domain path mapping template created with all 46 domains and current paths documented.
  </done>
</task>

</tasks>

<verification>
1. link-audit.md contains complete inventory of all 298+ links
2. Links are categorized by type (internal, cross-directory, navigation)
3. Each link has source file, line number, target path documented
4. link-update-strategy.md documents direct update approach
5. Strategy includes grep/sed commands for finding and updating links
6. Per-phase checklist template exists
7. Domain path mapping template created for all 46 domains
</verification>

<success_criteria>
- Complete link inventory exists with 298+ links documented
- All links categorized by type and migration action
- Link update strategy documented with clear processes
- Grep/sed commands provided for automation
- Domain path mapping template ready for migration phases
- Cross-directory links flagged for special attention
- Verification methods documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with:
- Link inventory summary (total counts by type)
- Key findings from audit
- How to use link-audit.md during migration
- Reference to link-update-strategy.md
</output>

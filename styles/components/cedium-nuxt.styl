& > img, & > p > img, & > figure> img, & > video, & > p > video, & > figure> video {
    box-sizing: border-box;
    display: block;
    margin: 1.5em auto;
    margin-bottom: 0;
    max-width: 100%;
    vertical-align: middle;
    border: solid 1px;

    & + figcaption {
        margin-top: 0.3em;
        margin-bottom: 0.5em;
        text-align: center;
        font-size: 12px;
        overflow: hidden;
    }
}

& > img, & > video {
    margin: 1.95rem auto 1.75em auto;
    max-width: 100%;

    // 图片链接失效时，若没有 alt 属性，则回退至显示 Emoji
    &[alt='']::after, &:not([alt])::after {
        content: '💔';
    }
}

u {
  text-decoration-line: underline;
  text-decoration-style: wavy;
  text-underline-offset: 0.2em;
}

blockquote {
  position: relative;
  margin: 1.5em;
  padding: 0 4.5em;
  border: 0;
  font-family: 'MJXTEX-I','楷体', fantasy, "Fang Song", "Kai";
  font-style: normal;
  font-size: 1.2em;

  & > p:nth-child(1) {
      position: relative;
      margin-top: 0;
      text-align: center;
      word-break: break-word;

      name {
          display: block;
          margin: auto;
          width: 95%;
          text-align: right;
          font-size: 1rem;

          &::before {
              content: '—— ';
          }
      }
  }

  & + p {
      margin-top: 0.8em;
  }
}

@media screen and (max-width: 888px) {
  blockquote {
    margin: 0;
    padding-left: 1em;
    padding-right: 1em;
  }
}
